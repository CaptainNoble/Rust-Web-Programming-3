[package]
name = "auth-actix-server"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4.13.0"
tokio = { version = "1.50.0", features = ["full"] }
glue = { path = "../../../../glue", features = ["actix"] }
auth-core = { path = "../../core" }
auth-dal = { path = "../../dal" }
